# 6. 现代控制知识框架速查

> @重点：先查维数，再查稳定性和结构性质，最后设计反馈、观测器与跟踪环节。

## 6.1 建模

$$
\dot x=Ax+Bu,\qquad y=Cx+Du.
$$

对一般微分方程

$$
y^{(n)}+a_{n-1}y^{(n-1)}+\cdots+a_1\dot y+a_0y=bu,
$$

取

$$
x_1=y,
\qquad x_2=\dot y,
\qquad \ldots,
\qquad x_n=y^{(n-1)}.
$$

则

$$
A=
\begin{bmatrix}
0&1&0&\cdots&0\\
0&0&1&\cdots&0\\
\vdots&\vdots&\vdots&\ddots&\vdots\\
0&0&0&\cdots&1\\
-a_0&-a_1&-a_2&\cdots&-a_{n-1}
\end{bmatrix},
$$

$$
B=
\begin{bmatrix}
0\\0\\\vdots\\0\\b
\end{bmatrix}.
$$

上方的一来自状态之间的逐级求导；末行负号来自原微分方程移项；B 的末项就是输入在最高阶方程中的系数。

## 6.2 状态响应

$$
x(t)=e^{At}x(0)+\int_0^t e^{A(t-\tau)}Bu(\tau)\,d\tau.
$$

## 6.3 传递函数与稳定性

$$
G(s)=C(sI-A)^{-1}B+D,
$$

连续系统渐近稳定要求

$$
\mathrm{Re}\bigl(\lambda_i(A)\bigr)<0.
$$

## 6.4 结构性质

$$
\mathcal C=[B\ AB\ \cdots\ A^{n-1}B],\qquad
\mathrm{rank}(\mathcal C)=n.
$$

$$
\mathcal O=\begin{bmatrix}C\\CA\\\vdots\\CA^{n-1}\end{bmatrix},\qquad
\mathrm{rank}(\mathcal O)=n.
$$

PBH 判据：

下列两种满秩条件分别对每个 $\lambda\in\sigma(A)$ 检查，分别对应完全能控、完全能观；并非随便取一个 $\lambda$ 检查即可。

$$
\operatorname{rank}[\lambda I-A\ \ B]=n,
\qquad
\operatorname{rank}\begin{bmatrix}\lambda I-A\\C\end{bmatrix}=n.
$$

## 6.5 控制与估计

任意极点配置先查完全能控；任意观测误差极点配置先查完全能观。下面观测器式默认 $D=0$，$e=x-\hat x$；若 $D\ne0$，创新项改为 $y-C\hat x-Du$。完整联合设计见第 11 份 11.9。

$$
u=-Kx\Rightarrow \dot x=(A-BK)x,
$$

$$
\dot{\hat x}=A\hat x+Bu+L(y-C\hat x)
\Rightarrow \dot e=(A-LC)e.
$$

## 6.6 前馈、最优控制与离散化

当

$$
D=0
$$

且直流增益可逆时，

$$
N=-[C(A-BK)^{-1}B]^{-1}.
$$

LQR：

采用无限时域连续 LQR，$Q\succeq0$、$R\succ0$；在 $(A,B)$ 可稳定化、$(Q^{1/2},A)$ 可检测的标准条件下取稳定化解 $P$，不能随便选 Riccati 方程的某个代数解。

$$
A^TP+PA-PBR^{-1}B^TP+Q=0,
\qquad K=R^{-1}B^TP.
$$

零阶保持离散化：

$$
A_d=e^{AT},\qquad B_d=\int_0^T e^{A\tau}B\,d\tau.
$$

## 6.7 稳定化、可检测与最小实现

- 可稳定化：不可控模态都稳定；
- 可检测：不可观模态都稳定；
- 最小实现：同时完全能控、完全能观。

> @易错：输入输出传递函数中发生极零相消，可能隐藏不稳定内部模态。

## 6.8 Gramian

$$
W_c(T)=\int_0^T e^{A\tau}BB^Te^{A^T\tau}\,d\tau,
$$

$$
W_o(T)=\int_0^T e^{A^T\tau}C^TCe^{A\tau}\,d\tau.
$$

有限时域 Gramian 正定分别等价于完全能控、完全能观。

## 6.9 积分伺服

$$
\dot\xi=r-y,
\qquad
u=-K_xx+K_i\xi.
$$

常值稳态调节方程：

$$
AX+BU=0,
\qquad CX+DU=I.
$$

> @重点：前馈校准名义跟踪，积分反馈抵抗恒值扰动和模型偏差；积分增广后要重新检查可控性。

## 6.10 离散状态空间

$$
x_k=A_d^kx_0+\sum_{i=0}^{k-1}A_d^{k-1-i}B_du_i,
\qquad \rho(A_d)<1.
$$

离散能控/能观矩阵形式不变，只把

$$
A,B,C
$$

换成

$$
A_d,B_d,C_d
$$

。离散 LQR 使用离散 Riccati 方程，不能套用连续公式。

详细目录见：[1. 文档目录](1.文档目录.md)。完整推导见：[5. 现代控制理论](5.现代控制理论（公理化推导与简例）.md)。

## 6.11 Lyapunov 与有限步控制补查

连续：$A^TP+PA=-Q$；离散：$A^TPA-P=-Q$。取 $Q\succ0$ 后，仍须验证解 $P\succ0$，不能只写“解得 $P$”就结束。二阶对称 $P$ 可用 $p_{11}>0$、$\det P>0$ 判断。

离散 $N$ 步转移的输入列顺序为 $[A^{N-1}B,\ldots,B]$，右端为 $x_f-A^Nx_0$。先判断线性方程有解，再求输入，最后逐步回代。示例与易错点见 [第 11 份 11.10—11.11](11.实战技巧手册（题型识别、完整步骤与易错补全）.md)。
