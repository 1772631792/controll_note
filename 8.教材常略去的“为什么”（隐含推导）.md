# 8. 教材常略去的“为什么”：隐含推导与得分点

这份笔记专门回答“公式为什么长这样”。每节按“结论从哪里来—考试写到哪一步—容易错在哪里”整理。遇到记不牢的结论，应从定义重新推出。

标记约定：@重点 是应能闭卷推导的骨架，@易错 是公式的适用边界。

## 8.1 为什么状态矩阵 \(A\) 上面是 0、1，最后一行是原方程系数

从一般微分方程开始：

$$
y^{(n)}+a_{n-1}y^{(n-1)}+\cdots+a_1\dot y+a_0y=bu.
$$

选相变量状态

$$
x_1=y,\quad x_2=\dot y,\quad \ldots,\quad x_n=y^{(n-1)}.
$$

逐个求导：

$$
\dot x_1=x_2,\quad \dot x_2=x_3,\quad \ldots,\quad \dot x_{n-1}=x_n.
$$

例如 \(\dot x_1=x_2\) 的系数形式是

$$
\dot x_1=0x_1+1x_2+0x_3+\cdots+0x_n.
$$

所以第一行必然是 \([0,1,0,\ldots,0]\)。其余各行同理，那个 1 只是把“下一阶导数”传给前一个状态，并不是背出来的常数。

最后一个状态满足 \(\dot x_n=y^{(n)}\)。由原微分方程移项：

$$
y^{(n)}=-a_0y-a_1\dot y-\cdots-a_{n-1}y^{(n-1)}+bu,
$$

因此

$$
\dot x_n=-a_0x_1-a_1x_2-\cdots-a_{n-1}x_n+bu.
$$

合并各行：

$$
\boxed{
A=
\begin{bmatrix}
0&1&0&\cdots&0\\
0&0&1&\cdots&0\\
\vdots&\vdots&\vdots&\ddots&\vdots\\
0&0&0&\cdots&1\\
-a_0&-a_1&-a_2&\cdots&-a_{n-1}
\end{bmatrix},
\qquad
B=\begin{bmatrix}0\\0\\\vdots\\0\\b\end{bmatrix}.
}
$$

> @重点：上方的 1 来自状态定义之间的逐级求导；末行负号来自原方程移项；\(B\) 的末项是输入在最高阶标准化方程中的真实系数。

> @易错：原方程最高阶项前若有 \(m\ne1\)，必须先把整个方程除以 \(m\)。例如 \(m\ddot y+c\dot y+ky=u\) 中，\(B\) 的末项是 \(1/m\)，不是 1。

## 8.2 为什么 \(C=[1,0,\ldots,0]\)，什么时候不是

输出方程是 \(y=Cx+Du\)。若状态定义中 \(x_1=y\)，则

$$
y=1x_1+0x_2+\cdots+0x_n,
$$

所以

$$
C=[1,0,\ldots,0],\qquad D=0.
$$

但 \(C\) 由“测量什么”决定，不由 \(A\) 自动决定。例如测量速度 \(y=\dot q=x_2\) 时，

$$
C=[0,1,0,\ldots,0].
$$

若输出为 \(y=2x_1-3x_2+0.5u\)，则

$$
C=[2,-3],\qquad D=0.5.
$$

> @重点：\(A\) 描述内部状态怎样互相影响，\(B\) 描述输入从哪里进入，\(C\) 描述哪些状态被测量，\(D\) 描述输入是否瞬时直通输出。

## 8.3 为什么传递函数的分子会进入 \(C\)

对严格真分式

$$
G(s)=\frac{b_{n-1}s^{n-1}+\cdots+b_1s+b_0}
{s^n+a_{n-1}s^{n-1}+\cdots+a_1s+a_0},
$$

取可控伴随型 \(A\) 和 \(B=[0,\ldots,0,1]^T\)。由 \(G(s)=C(sI-A)^{-1}B\) 可得

$$
(sI-A)^{-1}B
=\frac{1}{p(s)}
\begin{bmatrix}1\\s\\\vdots\\s^{n-1}\end{bmatrix}.
$$

若取

$$
C=[b_0,b_1,\ldots,b_{n-1}],
$$

则 \(C\) 与列向量相乘，恰好组成传递函数分子。

> @易错：这个 \(C\) 属于特定的可控标准型，不表示所有状态实现的 \(C\) 都等于分子系数。换状态坐标后 \(A,B,C\) 都会变，但传递函数不变。

若分子与分母同阶，先作多项式除法：

$$
G(s)=D+\frac{\text{严格真分式部分}}{p(s)}.
$$

商 \(D\) 是直接传递项；严格真分式的修正分子再进入 \(C\)。

## 8.4 为什么同一个系统可以有不同的 \(A,B,C\)

令新坐标 \(z=T^{-1}x\)，其中 \(T\) 可逆。因为 \(x=Tz\)，代入 \(\dot x=Ax+Bu\)：

$$
T\dot z=ATz+Bu.
$$

左乘 \(T^{-1}\)：

$$
\dot z=T^{-1}ATz+T^{-1}Bu,\qquad y=CTz+Du.
$$

所以

$$
\bar A=T^{-1}AT,\quad \bar B=T^{-1}B,\quad
\bar C=CT,\quad \bar D=D.
$$

相似变换不改变特征值，也不改变传递函数：

$$
\bar C(sI-\bar A)^{-1}\bar B+D
=C(sI-A)^{-1}B+D.
$$

> @重点：状态变量是内部坐标，不唯一；极点、传递函数、能控性和能观性才是坐标变换下不变的系统性质。

## 8.5 为什么状态响应里出现 \(e^{At}\)

仿照标量指数定义

$$
e^{At}=I+At+\frac{A^2t^2}{2!}+\cdots.
$$

逐项求导：

$$
\frac{d}{dt}e^{At}
=A+A^2t+\frac{A^3t^2}{2!}+\cdots
=Ae^{At}.
$$

且 \(e^{A0}=I\)，因此 \(x(t)=e^{At}x(0)\) 同时满足微分方程和初值。

对 \(\dot x=Ax+Bu\)，令 \(x=e^{At}v\)，代入并消去 \(Ae^{At}v\)，得 \(\dot v=e^{-At}Bu\)。积分后

$$
\boxed{x(t)=e^{At}x(0)+\int_0^te^{A(t-\tau)}Bu(\tau)\,d\tau.}
$$

## 8.6 为什么极点等于 \(A\) 的特征值

零初值拉普拉斯变换给出

$$
X(s)=(sI-A)^{-1}BU(s).
$$

矩阵逆可写成

$$
(sI-A)^{-1}=\frac{\operatorname{adj}(sI-A)}{\det(sI-A)}.
$$

其分母为 \(\det(sI-A)\)，令其为零正是 \(A\) 的特征方程。因此内部自然模态由 \(A\) 的特征值决定。

> @易错：传递函数中可能出现极零相消，使某些特征值看不见。只有最小实现中，传递函数极点才与 \(A\) 的全部特征值一一对应。

## 8.7 为什么状态反馈后是 \(A-BK\)

取控制律 \(u=-Kx+Nr\)，直接代入：

$$
\dot x=Ax+B(-Kx+Nr)
=(A-BK)x+BNr.
$$

因此闭环内部矩阵是 \(A-BK\)。负号来自所选负反馈控制律；若控制律定义成 \(u=Kx\)，公式自然变成 \(A+BK\)。

> @重点：不是反馈直接修改 \(A\)；而是输入先乘 \(B\)，所以反馈产生的修正量必然是 \(BK\)。

## 8.8 为什么观测误差矩阵是 \(A-LC\)

全维观测器为

$$
\dot{\hat x}=A\hat x+Bu+L(y-C\hat x).
$$

定义 \(e=x-\hat x\)。由于 \(y=Cx\)，对象方程与观测器相减：

$$
\dot e=Ax+Bu-A\hat x-Bu-L(Cx-C\hat x)
=(A-LC)e.
$$

输入项 \(Bu\) 被消去，是因为对象和观测器使用同一个已知输入。输出残差通过 \(L\) 注入，用来纠正估计。

> @易错：若 \(D\ne0\)，创新项应写成 \(y-C\hat x-Du\)，否则输入直通项会被误当成状态估计误差。

## 8.9 为什么能控矩阵是 \([B,AB,\ldots,A^{n-1}B]\)

输入经过 \(B\) 进入系统，随后被内部动力学 \(A\) 传播，因此可能到达的方向依次是

$$
B,\ AB,\ A^2B,\ \ldots.
$$

由 Cayley--Hamilton 定理，更高次幂不会增加新方向。所有可达方向张成

$$
\operatorname{span}\{B,AB,\ldots,A^{n-1}B\}.
$$

若其秩为 \(n\)，这些方向覆盖整个状态空间：

$$
\boxed{\mathcal C=[B\ AB\ \cdots\ A^{n-1}B],\qquad
\operatorname{rank}\mathcal C=n.}
$$

> @重点：\(B\) 是输入直接推动的方向，\(AB\) 是该影响经过一次内部传播后的方向；能控矩阵不是凭空拼接出来的。

## 8.10 为什么能观矩阵是 \([C;CA;\ldots;CA^{n-1}]\)

先看零输入系统 \(y=Cx\)。对输出求导：

$$
\dot y=CAx,\qquad \ddot y=CA^2x.
$$

把初始时刻的输出及其导数叠起来：

$$
\begin{bmatrix}
y(0)\\\dot y(0)\\\vdots\\y^{(n-1)}(0)
\end{bmatrix}
=
\begin{bmatrix}
C\\CA\\\vdots\\CA^{n-1}
\end{bmatrix}x(0).
$$

若右侧矩阵满列秩，初始状态可由输出信息唯一恢复；若不满秩，就存在某个非零状态方向始终不可见。

> @重点：能控性沿 \(B\to AB\to\cdots\) 向外传播；能观性沿 \(C\to CA\to\cdots\) 把内部状态投影到输出。

## 8.11 为什么零阶保持离散化是 \(A_d=e^{AT}\)、\(B_d=\int e^{A\tau}B\,d\tau\)

连续状态响应在一个采样区间上为

$$
x((k+1)T)=e^{AT}x(kT)
+\int_{kT}^{(k+1)T}e^{A((k+1)T-\tau)}Bu(\tau)\,d\tau.
$$

零阶保持器让区间内输入恒为 \(u_k\)。更换积分变量后

$$
x_{k+1}=e^{AT}x_k+
\left(\int_0^Te^{A\tau}B\,d\tau\right)u_k.
$$

所以

$$
\boxed{A_d=e^{AT},\qquad B_d=\int_0^Te^{A\tau}B\,d\tau.}
$$

> @易错：一般不能写成 \(A_d=I+AT,\ B_d=BT\)。那只是小采样周期下的一阶近似。

## 8.12 为什么负反馈分母是 \(1+GH\)

负反馈结构满足

$$
e=r-Hy,\qquad y=Ge.
$$

代入并移项：

$$
y=G(r-Hy)=Gr-GHy,
$$

$$
(1+GH)y=Gr.
$$

因此

$$
\boxed{\frac{Y}{R}=\frac{G}{1+GH}.}
$$

分母中的加号来自负反馈关系里的减号在移项后变成加号。若结构本身是正反馈，分母才是 \(1-GH\)。

## 8.13 为什么系统型别决定阶跃、斜坡、抛物线误差

单位反馈下

$$
E(s)=\frac{R(s)}{1+G(s)}.
$$

若开环在原点有 \(\nu\) 个积分环节，低频 \(G(s)\sim K/s^\nu\)。对 \(m\) 阶多项式输入，\(R(s)\) 与 \(1/s^{m+1}\) 成正比。由终值定理

$$
e_{ss}\sim\lim_{s\to0}
\frac{s}{1+K/s^\nu}\frac{1}{s^{m+1}}
\sim\frac{1}{K}s^{\nu-m}.
$$

于是：

- \(\nu>m\)：误差趋于 0；
- \(\nu=m\)：误差为有限常数；
- \(\nu<m\)：误差发散。

这就是 0、1、2 型系统分别对应位置、速度、加速度误差系数的来源。

> @易错：这套结论依赖闭环稳定和低频极限存在；不能只数积分环节而跳过稳定性检查。

## 8.14 为什么二阶系统的超调量是指数公式

标准欠阻尼二阶系统极点为

$$
s_{1,2}=-\zeta\omega_n\pm j\omega_n\sqrt{1-\zeta^2}.
$$

记 \(\omega_d=\omega_n\sqrt{1-\zeta^2}\)。阶跃响应的振荡包络按 \(e^{-\zeta\omega_nt}\) 衰减，第一次峰值发生在

$$
t_p=\frac{\pi}{\omega_d}.
$$

代入指数包络：

$$
\boxed{M_p=e^{-\zeta\omega_nt_p}
=e^{-\frac{\pi\zeta}{\sqrt{1-\zeta^2}}}.}
$$

\(\omega_n\) 被约掉，所以百分比超调主要由阻尼比决定；\(\omega_n\) 主要控制时间尺度。

## 8.15 为什么调节时间近似为 \(4/(\zeta\omega_n)\)

误差包络近似按 \(e^{-\zeta\omega_nt}\) 衰减。采用 2% 误差带：

$$
e^{-\zeta\omega_nt_s}\approx0.02,
$$

$$
t_s\approx\frac{-\ln0.02}{\zeta\omega_n}
\approx\frac{3.91}{\zeta\omega_n}
\approx\frac{4}{\zeta\omega_n}.
$$

5% 误差带给出 \(t_s\approx3/(\zeta\omega_n)\)。

> @重点：常数 4 不是固定定理，而是 2% 误差带与指数包络近似共同产生的。

## 8.16 为什么根轨迹有角度条件和幅值条件

闭环特征方程 \(1+KG_0(s)=0\)，所以根轨迹上的点满足

$$
KG_0(s)=-1=1\angle(2k+1)\pi.
$$

比较幅值与相角：

$$
\boxed{\angle G_0(s)=(2k+1)\pi,\qquad
K=\frac{1}{|G_0(s)|}.}
$$

角度条件决定某点是否在根轨迹上，幅值条件决定对应增益。

## 8.17 为什么 Bode 图每个一阶极点改变 \(-20\) dB/dec

一阶极点因子为

$$
\frac{1}{1+j\omega/\omega_p}.
$$

当 \(\omega\gg\omega_p\) 时，其幅值近似为 \(\omega_p/\omega\)。取分贝：

$$
20\log_{10}\frac{\omega_p}{\omega}
=-20\log_{10}\frac{\omega}{\omega_p}.
$$

频率增大 10 倍，对数增加 1，幅值下降 20 dB，因此斜率是 \(-20\) dB/dec。一个一阶零点贡献 \(+20\) dB/dec；二阶因子贡献 \(\pm40\) dB/dec。

## 8.18 为什么 Nyquist 判据要数 \(-1\) 点

闭环特征方程是 \(1+L(s)=0\)。辩值原理说明：沿 Nyquist 围线走一周，\(1+L(s)\) 绕原点的净圈数等于其右半平面零点数减去极点数。

把曲线 \(1+L(s)\) 平移后看成 \(L(s)\)，“\(1+L\) 绕原点”等价于“\(L\) 绕 \(-1\) 点”。

若采用顺时针包围为正的约定，

$$
\boxed{N=Z-P.}
$$

\(P\) 是开环右半平面极点数，\(Z\) 是闭环右半平面极点数；闭环稳定要求 \(Z=0\)。

> @易错：不同教材对方向和 \(N\) 的正负定义可能相反。不要孤立背公式，必须先声明符号约定。

## 8.19 一页式“为什么”答题模板

遇到教材直接给公式的题，可按以下顺序补齐得分步骤：

1. 写定义：状态怎么选、误差怎么定义、输入输出关系是什么；
2. 逐行展开：先写标量方程，再合并成矩阵；
3. 标出来源：1 来自逐级求导，负号来自移项，\(B\) 来自输入位置；
4. 写成立条件：零初值、闭环稳定、严格真分式、\(D=0\) 或采样保持方式；
5. 最后验算：矩阵维数、特征多项式、直流增益和稳定区域。

> @重点：真正能稳定得分的不是多背一个公式，而是能从“定义 → 代入 → 移项 → 合并”四步把公式重新造出来。
