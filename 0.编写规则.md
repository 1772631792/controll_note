# 0. 编写规则

适用于本目录的 Markdown 笔记、独立专题、题解及其配图。目标是让读者能理解公式从何而来、何时能用，并在 Typora 中完整看到公式。

## 0.1 内容与讲解顺序

1. 先说具体问题：这个公式想解决什么，直接计算为什么麻烦。
2. 列出先修知识、符号、模型和成立条件，不能把零初值、单位反馈、连续时间等默认条件藏起来。
3. 用一个最小例子建立直觉，再从定义或基本方程逐步推导。
4. 每个关键等号说明所做的运算。引用定理时明确标为定理，不把直观解释冒充完整证明。
5. 至少给一个带条件、计算过程和答案的完整例题，并做独立回代。
6. 至少给一个边界或反例，说明条件改变后为什么不能直接套用。
7. 结尾写出考场识别线索、最短可靠步骤与回查入口。

“容易机械记忆”是编写时对学习障碍的判断，不写成未经调查的“多数人都不会”。难点专题统一集中在 [第 12 份难点专题详解](12.难点专题详解（讲解、配图、例题与公式）.md)这一份 Markdown 中，每个难点作为单独章节，直接包含讲解、配图、题目和公式，不再拆成多个专题文件让读者来回跳转。相关原理可以补充回查链接，但不能用链接代替本节核心讲解。

本次用户另行指定的第 14 份“缺口补齐”和第 15 份“冲击高分”作为独立文件保留；这是独立编排的明确例外。两份沿用本规则的公式、题源、配图和核验要求，并在总目录互相链接。

## 0.2 Typora 公式规则

**数学表达式统一使用独立公式块，不依赖行内数学开关。** 禁止在正文、标题、普通 Markdown 表格中用单个美元符号包围公式。短变量可以写成明确的中文名称；需要数学排版时也放入独立公式块。

公式块的起止标记各占一行，前后留空行：

```text
先写状态方程：

$$
\dot x=Ax+Bu.
$$

再根据初值求解。
```

不要把起止标记放在标题末尾、句子后面或表格单元格里。公式块内不能再嵌套美元符号。

## 0.3 公式内的文字、矩阵与换行

短说明可以放进公式块，使用 `\text{说明}`，不要直接把整段中文当数学符号输入。例如：

$$
\begin{gathered}
\text{取}\quad Q=I,\qquad A^TP+PA=-I;\\
P>0\quad\Rightarrow\quad\text{原点渐近稳定。}
\end{gathered}
$$

长段解释留在正文。公式块不会像普通段落一样可靠地自动折行，因此多个条件、长推导使用 `aligned` 或 `gathered` 显式换行。示例：

```text
$$
\begin{aligned}
\dot V&=\dot x^TPx+x^TP\dot x\\
&=x^T(A^TP+PA)x.
\end{aligned}
$$
```

- 矩阵用 `bmatrix` 或 `pmatrix`；列间使用 `&`，行间使用 `\\`。
- 分段公式用 `cases`，推导对齐用 `aligned`，多行并列结论用 `gathered`。
- 含公式的表格改成独立 `array` 公式块，或拆成“中文表格＋表外公式”；不要在 Markdown 单元格内塞公式块。
- 函数用 `\sin`、`\ln`、`\det`、`\operatorname{rank}` 等标准写法。
- 单位用直立字体，短中文用 `\text{}`；百分号写成 `\%`。
- `\text{}` 内需要显示特殊字符时转义，例如 `\&`、`\_`、`\#`。
- 不使用未经验证的自定义宏。每个 `\begin` 必须有对应 `\end`，每个括号组必须闭合。

矩阵示例：

$$
P=\begin{bmatrix}5/4&1/4\\1/4&1/4\end{bmatrix},\qquad
\det P=1/4>0.
$$

## 0.4 公式与结论的正确性

- 区分传递函数外部稳定性和内部状态稳定性，不能约消不稳定模态后直接宣布内部稳定。
- 求有限稳态值前检查终值定理的极点条件；闭环稳定不保证所有输入对应的误差均有有限终值。
- 明确连续或离散、开环或闭环、参考或扰动通道，保留反馈和扰动的正负号。
- 数值计算保留足够中间精度，区分精确结果、近似公式和图上估计。
- 参数范围的等号、零分母、降阶、不可逆矩阵等情况单独检查。
- 图示只解释机制；有限轨迹的仿真不能取代理论证明。
- 同一公式出现不同教材写法时，先列出每种写法的符号定义、方向、频率范围、归一化和适用条件，再给等价变换；不能只堆公式让读者猜。
- 绕数与穿越计数必须注明顺逆时针、完整曲线或正频率半边、半次端点及虚轴极点补弧。若换了时间类型、误差定义或控制律，要明确说明这不是简单换字母。
- 零频、高频、等号边界应从极限或局部展开推导；遇到主导项无法判断出发侧时继续展开，不能只凭相角极限画图。

## 0.5 真题、自编例题与来源

真题标明学校、年份、科目和题号，并链接已有原卷。没有原卷时标明题意转述或待核验；不推测补成“原题已知”。新增数值练习标为自编例题。

从现有答案反推的条件必须明确写出推断过程。外部资料的结论与自己的推导分开表达，不将机构解析视为官方答案。

## 0.6 Python 配图规则

本次使用全局解释器 `D:\Python310\python.exe`，不为配图单独建立虚拟环境。生成脚本放在 `scripts`，图片放在 `assets` 下的专题目录，并在正文用相对路径嵌入。

```powershell
& 'D:\Python310\python.exe' scripts/generate_difficult_concepts.py
& 'D:\Python310\python.exe' scripts/verify_formula_conventions.py
```

1. 数学曲线和示意图用 Python、NumPy、SciPy、Matplotlib 等可计算工具生成。
2. 必须保留生成脚本、参数和输出文件名，使图片可以重建；不能只留下无法修改的截图。
3. 中文字体显式设置，公式标签使用 Matplotlib 支持的数学语法；不要照搬复杂 LaTeX 环境到绘图库。
4. 坐标、单位、参数、箭头方向和图例明确；不同曲线同时用线型或标记区分。
5. 零极点图注明实轴、虚轴及稳定区域；频率图注明频率单位、相位单位和穿越方向。
6. 默认输出适合 Typora 的 PNG；需要可缩放版本可同时输出 SVG。图片宽度、字号和留白必须适合阅读。
7. 每张图配“怎么看”和“不能从图推出什么”，避免只插图不解释。
8. 生成后检查实际图像，不能只凭脚本成功退出就宣布图形正确。

## 0.7 修改后的检查

1. 扫描所有正文，确保没有残留的行内公式；规则文档中的代码示例除外。
2. 检查公式块、环境和括号配对；可用 MathJax 做语法解析并检查过宽公式。
3. 对新增算例用代回方程、符号运算或独立数值计算验证。
4. 检查 Markdown 的本地图片与文件链接是否存在。
5. 检查配图中的标签、遮挡、裁切、箭头及曲线是否与正文一致。
6. 更新第 1 份目录和第 12 份难点合订本的内部导航。修改已有文件前先读取内容，保留用户已有修改，不覆盖其他工作的成果。

MathJax 解析通过说明公式语法可解析，不等同于已在用户的 Typora 窗口逐页检查；交付时如实区分两种验证。
