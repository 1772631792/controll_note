# 8. 厦门大学 844 近三年真题解析

## 8.0 目录与说明

1. [8.1 2023 年](#81-2023-年)
2. [8.2 2024 年](#82-2024-年)
3. [8.3 2025 年](#83-2025-年)
4. [8.4 2023—2024 考点归纳与 2025 衔接](#84-20232024-考点归纳与-2025-衔接)

说明：本稿按题面重新推导，不照抄所附机构答案。2023、2024 年解析保留在本文；2025 年已有单独的逐题详解，为避免重复，本文只提供入口和三年衔接。

---

## 8.1 2023 年

### 8.1.1 第一题：有源 PI 电路的状态空间模型

取两个电容电压为状态：

$$
x_1=u_{C_0},\qquad x_2=u_{C_1},\qquad u=u_r,\qquad y=u_o.
$$

理想运算放大器满足虚短、虚断：

$$
u_+=u_-=0,\qquad i_+=i_-=0.
$$

在第一个节点列 KCL：

$$
\frac{u_r-u_{C_0}}{R_0}
=C_0\dot u_{C_0}+\frac{u_{C_0}}{R_0}.
$$

故

$$
\dot x_1=-\frac{2}{R_0C_0}x_1+\frac{1}{R_0C_0}u.
$$

反相端不吸收电流，因此第二个电阻的电流全部流入反馈支路：

$$
C_1\dot u_{C_1}=\frac{u_{C_0}}{R_0}.
$$

故

$$
\dot x_2=\frac{1}{R_0C_1}x_1.
$$

沿反馈支路列电压关系：

$$
u_o=-\frac{R_1}{R_0}u_{C_0}+u_{C_1}.
$$

最终模型为

$$
\dot x=
\begin{bmatrix}
-\dfrac{2}{R_0C_0}&0\\
\dfrac{1}{R_0C_1}&0
\end{bmatrix}x+
\begin{bmatrix}
\dfrac{1}{R_0C_0}\\
0
\end{bmatrix}u,
$$

$$
y=
\begin{bmatrix}
-\dfrac{R_1}{R_0}&1
\end{bmatrix}x.
$$

易错点：状态方程的量纲必须含电容，不能把分母误写成电感。

### 8.1.2 第二题：带测速反馈的根轨迹

把反馈环节并入开环，等效开环传递函数为

$$
L(s)=\frac{K(1+K_ts)}{s(s+1)}.
$$

闭环特征方程为

$$
s^2+(1+KK_t)s+K=0.
$$

#### 1. 参数分区

当

$$
K_t=0,
$$

开环极点为零和负一，无有限零点。实轴根轨迹为负一到零，分离点为

$$
d=-\frac12,
$$

两条渐近线角度为正负九十度，交点为

$$
\sigma_a=-\frac12.
$$

当

$$
0<K_t<1,
$$

新增零点为

$$
z=-\frac1{K_t}<-1.
$$

复根满足根与系数关系：

$$
s_{1,2}=x\pm jy,
$$

$$
2x=-(1+KK_t),\qquad x^2+y^2=K.
$$

消去 K 得

$$
\left(x+\frac1{K_t}\right)^2+y^2
=\frac{1-K_t}{K_t^2}.
$$

所以复数段是圆的一部分，圆心与半径分别为

$$
\left(-\frac1{K_t},0\right),\qquad
\frac{\sqrt{1-K_t}}{K_t}.
$$

当

$$
K_t>1,
$$

零点位于负一与零之间，判别式始终非负，两条闭环极点均在实轴上运动。

#### 2. 数值代入

取

$$
K_t=0.5,\qquad K=10,
$$

则

$$
\Phi(s)=\frac{10}{s^2+6s+10},
$$

$$
s_{1,2}=-3\pm j.
$$

与标准二阶式比较：

$$
\omega_n=\sqrt{10}=3.162,
$$

$$
\zeta=\frac{6}{2\sqrt{10}}=0.949.
$$

超调量与百分之五调节时间近似为

$$
M_p=e^{-\pi\zeta/\sqrt{1-\zeta^2}}\times100\%
\approx0.0069\%,
$$

$$
t_s\approx\frac{3}{\zeta\omega_n}=1.00\ \mathrm{s}.
$$

### 8.1.3 第三题：由校正后 Bode 图反求校正器

由斜率变化反推原开环模型：

$$
G(s)=
\frac{K(s+1)\left(1+\dfrac{s}{5}\right)}
{s\left(1+\dfrac{s}{0.2}\right)
\left(1+\dfrac{s}{10}\right)
\left(1+\dfrac{s}{80}\right)}.
$$

由图中四十弧度每秒处过零分贝，按渐近线计算得

$$
K=100.
$$

超调量为百分之十六点三，对应

$$
\zeta=0.5.
$$

单位速度误差为零点二，因此

$$
K_v=5.
$$

选取标准二阶校正后开环模型

$$
G_0(s)=\frac{\omega_n^2}{s(s+2\zeta\omega_n)}.
$$

由于

$$
K_v=\lim_{s\to0}sG_0(s)=\frac{\omega_n}{2\zeta}=5,
$$

得到

$$
\omega_n=5,
$$

$$
G_0(s)=\frac{25}{s(s+5)}.
$$

故串联校正器为

$$
G_c(s)=\frac{G(s)}{G_0(s)}
=\frac{20(s+1)\left(1+\dfrac{s}{5}\right)^2}
{\left(1+\dfrac{s}{0.2}\right)
\left(1+\dfrac{s}{10}\right)
\left(1+\dfrac{s}{80}\right)}.
$$

校正前近似有

$$
\omega_{c0}=5\ \mathrm{rad/s},\qquad \gamma_0=45^\circ.
$$

校正后由图读得

$$
\omega_c=40\ \mathrm{rad/s},\qquad \gamma\approx69^\circ.
$$

### 8.1.4 第四题：PID 指标题的题面矛盾

由幅频图的低频二十分贝平台和三个转折点，可重构校正前开环模型：

$$
G_0(s)=
\frac{10}
{(s+1)\left(1+\dfrac{s}{5}\right)
\left(1+\dfrac{s}{20}\right)}.
$$

近似截止频率和相位裕度为

$$
\omega_{c0}\approx7.07\ \mathrm{rad/s},
$$

$$
\gamma_0
=180^\circ-\arctan\omega_{c0}
-\arctan\frac{\omega_{c0}}5
-\arctan\frac{\omega_{c0}}{20}
\approx23.85^\circ.
$$

指标给出

$$
M_p=16.3\%,\qquad t_s=0.7\ \mathrm{s},
$$

故取

$$
\zeta=0.5,\qquad \omega_n=10.
$$

目标开环可取

$$
G_d(s)=\frac{100}{s(s+10)}.
$$

按模型匹配得到

$$
G_c(s)=\frac{G_d(s)}{G_0(s)}
=\frac{(s+1)\left(1+\dfrac{s}{5}\right)
\left(1+\dfrac{s}{20}\right)}
{s\left(1+\dfrac{s}{10}\right)}.
$$

结论：该校正器含额外零极点，不是

$$
K_P+\frac{K_I}{s}+K_Ds
$$

的严格 PID 形式。因此仅凭题图无法唯一确定三个 PID 参数；原题“精确 PID 且精确匹配二阶模型”的要求互相冲突。

### 8.1.5 第五题：对象虚焊下的鲁棒校正

正常对象与虚焊对象分别为

$$
P(s)=\frac1{s+2},\qquad P_1(s)=\frac1s.
$$

阶跃零稳态误差要求环路至少含一个积分环节。取最简单的 PI 型校正器

$$
G_c(s)=\frac{K(s+a)}s.
$$

虚焊对象的环路为

$$
L_1(s)=\frac{K(s+a)}{s^2}.
$$

为了留出余量，选

$$
\omega_{c1}=8\ \mathrm{rad/s},\qquad \gamma_1=45^\circ.
$$

相位条件为

$$
-180^\circ+\arctan\frac{\omega_{c1}}a=-135^\circ,
$$

故

$$
a=8.
$$

幅值条件为

$$
\frac{K\sqrt{\omega_{c1}^2+a^2}}{\omega_{c1}^2}=1,
$$

故

$$
K=\frac{8}{\sqrt2}=5.66.
$$

校正器可取

$$
G_c(s)=\frac{5.66(s+8)}s.
$$

对正常对象重新计算得

$$
\omega_c\approx7.84\ \mathrm{rad/s},\qquad
\gamma\approx58.7^\circ.
$$

两种对象均满足要求。

### 8.1.6 第六题：时滞系统的 Nyquist 稳定范围

开环为

$$
G(s)=\frac{Ke^{-0.8s}}{s+1}.
$$

频率响应的幅值与相位为

$$
|G(j\omega)|=\frac{K}{\sqrt{1+\omega^2}},
$$

$$
\angle G(j\omega)=-\arctan\omega-0.8\omega.
$$

临界稳定时相位为负一百八十度：

$$
\arctan\omega_g+0.8\omega_g=\pi.
$$

数值解为

$$
\omega_g\approx2.45\ \mathrm{rad/s}.
$$

再令幅值为一：

$$
K_{\mathrm{cr}}=\sqrt{1+\omega_g^2}\approx2.65.
$$

所以闭环稳定范围为

$$
0<K<2.65.
$$

### 8.1.7 第七题：离散系统能控性与有限步转移

系统矩阵为

$$
A=
\begin{bmatrix}
1&0&0\\
0&2&-2\\
-1&1&0
\end{bmatrix},\qquad
B=
\begin{bmatrix}
1\\0\\1
\end{bmatrix}.
$$

能控矩阵为

$$
\mathcal C=
\begin{bmatrix}
B&AB&A^2B
\end{bmatrix}
=
\begin{bmatrix}
1&1&1\\
0&-2&-2\\
1&-1&-3
\end{bmatrix}.
$$

$$
\det\mathcal C=4\ne0.
$$

故系统完全能控。

由

$$
x(3)=A^3x(0)+A^2Bu(0)+ABu(1)+Bu(2)=0
$$

解得

$$
u(0)=-5,\qquad u(1)=11,\qquad u(2)=-8.
$$

若要求两步到达原点，则

$$
x(2)=A^2x(0)+ABu(0)+Bu(1)=0.
$$

矩阵

$$
\begin{bmatrix}AB&B\end{bmatrix}
$$

只有两列，不能覆盖所有三维初态。其左零向量可取

$$
q=
\begin{bmatrix}
1\\1\\-1
\end{bmatrix}.
$$

可达条件为

$$
q^TA^2x(0)=0,
$$

即

$$
4x_1(0)-2x_3(0)=0.
$$

所以仅当

$$
x_3(0)=2x_1(0)
$$

时，才能在两步内到达原点。

### 8.1.8 第八题：传递函数的并联实现

给定

$$
W(s)=\frac{4s^2+10s+5}{s^3+5s^2+8s+4}.
$$

分母分解并作部分分式展开：

$$
W(s)=-\frac1{s+1}-\frac1{(s+2)^2}+\frac5{s+2}.
$$

一种并联实现为

$$
\dot x=
\begin{bmatrix}
-1&0&0\\
0&-2&1\\
0&0&-2
\end{bmatrix}x+
\begin{bmatrix}
1\\0\\1
\end{bmatrix}u,
$$

$$
y=
\begin{bmatrix}
-1&-1&5
\end{bmatrix}x.
$$

其中二重极点用二阶 Jordan 链实现。

### 8.1.9 第九题：非线性系统的精确抵消跟踪

题面系统为

$$
\ddot y=y-2\dot y+2y\sin y+u.
$$

要求只使用可测输出 y，使单位阶跃参考输入能无静差跟踪。直接抵消不稳定线性项和非线性项，取

$$
u=r-2y-2y\sin y.
$$

代回原系统：

$$
\ddot y+2\dot y+y=r.
$$

闭环传递函数为

$$
\frac{Y(s)}{R(s)}=\frac1{s^2+2s+1}=\frac1{(s+1)^2}.
$$

闭环稳定，且直流增益为一：

$$
\lim_{t\to\infty}y(t)=1.
$$

易错点：只在线性化模型上配置极点，不能保证原非线性项被消除；题目允许直接测量 y，所以上式是更简单且精确的做法。

### 8.1.10 第十题：一阶热过程参数辨识

采用增量模型：

$$
\frac{\Delta T(s)}{\Delta P(s)}=\frac{K}{\tau s+1}.
$$

输入功率由一点零千瓦变为一点五千瓦：

$$
\Delta P=0.5\ \mathrm{kW}.
$$

温度由八十摄氏度最终升到二百八十摄氏度：

$$
\Delta T(\infty)=200^\circ\mathrm C.
$$

因此

$$
K=\frac{200}{0.5}=400\ ^\circ\mathrm C/\mathrm{kW}.
$$

四分钟时的温升为

$$
\Delta T(4)=230-80=150^\circ\mathrm C.
$$

一阶阶跃响应满足

$$
150=0.5\times400\left(1-e^{-4/\tau}\right).
$$

所以

$$
e^{-4/\tau}=0.25,
$$

$$
\tau=\frac4{\ln4}=2.885\ \mathrm{min}.
$$

最终模型为

$$
\frac{\Delta T(s)}{\Delta P(s)}
=\frac{400}{2.885s+1}.
$$

---

## 8.2 2024 年

### 8.2.1 第一题：RC 网络的传递函数与方框图

电阻与电容并联支路的阻抗为

$$
Z_1=R_1\parallel\frac1{C_1s}
=\frac{R_1}{1+R_1C_1s}.
$$

输出取电阻 R₂ 两端，由分压公式得

$$
\frac{U_0(s)}{U_i(s)}
=\frac{R_2}{R_2+Z_1}.
$$

化简：

$$
\frac{U_0(s)}{U_i(s)}
=\frac{C_1R_1R_2s+R_2}
{C_1R_1R_2s+R_1+R_2}.
$$

可写成标准一阶零极点形式：

$$
\frac{U_0(s)}{U_i(s)}
=\frac{R_2}{R_1+R_2}
\frac{1+R_1C_1s}
{1+\dfrac{R_1R_2C_1}{R_1+R_2}s}.
$$

方框图按“常数增益—一阶零点—一阶极点”三个串联环节画出即可。

### 8.2.2 第二题：由阶跃曲线辨识二阶参数

闭环传递函数为

$$
\Phi(s)=
\frac{K_1K_2}{s^2+as+K_2}.
$$

与标准二阶形式比较：

$$
\Phi(s)=\frac{b\omega_n^2}
{s^2+2\zeta\omega_ns+\omega_n^2}.
$$

由终值二点零得到

$$
K_1=2.
$$

超调量为

$$
M_p=\frac{2.18-2}{2}=0.09.
$$

利用

$$
M_p=e^{-\pi\zeta/\sqrt{1-\zeta^2}}
$$

求得

$$
\zeta\approx0.608.
$$

峰值时间为

$$
t_p=\frac{\pi}{\omega_n\sqrt{1-\zeta^2}}=0.8,
$$

故

$$
\omega_n\approx4.946\ \mathrm{rad/s}.
$$

再由系数对应关系

$$
K_2=\omega_n^2,\qquad a=2\zeta\omega_n,
$$

得到

$$
K_2\approx24.463,
$$

$$
a\approx6.014.
$$

### 8.2.3 第三题：特殊行 Routh 表与全部特征根

特征多项式为

$$
D(s)=s^6+s^5-2s^4-3s^3-7s^2-4s-4.
$$

Routh 表出现全零行，其上一行对应辅助多项式

$$
F(s)=s^4-3s^2-4.
$$

用导数

$$
F'(s)=4s^3-6s
$$

替换全零行后继续计算。第一列发生一次变号，且辅助多项式含纯虚根，所以系统不稳定。

直接因式分解可核对全部根：

$$
D(s)=(s^2+1)(s-2)(s+2)(s^2+s+1).
$$

故

$$
s=\pm j,\quad s=2,\quad s=-2,
$$

$$
s=-\frac12\pm j\frac{\sqrt3}{2}.
$$

### 8.2.4 第四题：三极点系统根轨迹

开环为

$$
G(s)=\frac{10K}{s(s+2)(s+5)}.
$$

极点为零、负二、负五，无有限零点。实轴根轨迹为

$$
(-2,0)\cup(-\infty,-5).
$$

渐近线交点与角度为

$$
\sigma_a=-\frac73,
$$

$$
\theta=60^\circ,\ 180^\circ,\ 300^\circ.
$$

由

$$
K=-\frac{s(s+2)(s+5)}{10}
$$

求驻点：

$$
3s^2+14s+10=0.
$$

两根为

$$
s=\frac{-7\pm\sqrt{19}}3.
$$

只有

$$
s\approx-0.880
$$

位于实轴根轨迹上，对应

$$
K\approx0.406.
$$

令闭环特征式

$$
s^3+7s^2+10s+10K=0
$$

中的 s 取纯虚数，得到

$$
K=7,
$$

$$
s=\pm j\sqrt{10}.
$$

### 8.2.5 第五题：开环对数幅频曲线

开环为

$$
G(s)=
\frac{7.5\left(1+\dfrac{s}{3}\right)}
{s\left(1+\dfrac{s}{2}\right)
\left(1+\dfrac{s}{2}+\dfrac{s^2}{2}\right)}.
$$

二阶因子的参数为

$$
\omega_n=\sqrt2,\qquad
\zeta=\frac1{2\sqrt2}.
$$

转折频率依次为

$$
\sqrt2,\qquad 2,
\qquad 3\quad \mathrm{rad/s}.
$$

各段总斜率依次为

$$
-20,\qquad -60,\qquad -80,\qquad -60
\quad \mathrm{dB/dec}.
$$

低频渐近线在一弧度每秒处为

$$
20\log_{10}7.5\approx17.50\ \mathrm{dB}.
$$

按连续折线计算，三个转折点附近的渐近幅值约为

$$
14.49\ \mathrm{dB},\qquad
5.46\ \mathrm{dB},\qquad
-8.63\ \mathrm{dB}.
$$

精确求解

$$
|G(j\omega_c)|=1
$$

得到

$$
\omega_c\approx2.493\ \mathrm{rad/s}.
$$

### 8.2.6 第六题：满足指定截止频率的超前校正

原开环为

$$
G_0(s)=\frac{K}{s(0.25s+1)(0.01s+1)}.
$$

由于

$$
K_v=\lim_{s\to0}sG_0(s)=K=50,
$$

取

$$
K=50.
$$

要求校正后截止频率为

$$
\omega_c=25\ \mathrm{rad/s}.
$$

在该频率，原系统精确幅值为

$$
|G_0(j25)|
=\frac{50}{25\sqrt{1+(0.25\times25)^2}
\sqrt{1+(0.01\times25)^2}}
\approx0.30655.
$$

选超前校正器

$$
G_c(s)=\frac{1+s/\omega_z}{1+s/\omega_p},
\qquad \omega_z<\omega_p.
$$

令二十五弧度每秒为最大超前频率，则

$$
\omega_z\omega_p=25^2.
$$

再令该频率的幅值补偿恰好为原幅值的倒数：

$$
\sqrt\alpha=\frac1{|G_0(j25)|},
\qquad
\alpha=\frac{\omega_p}{\omega_z}.
$$

得到

$$
\alpha\approx10.642,
$$

$$
\omega_z=\frac{25}{\sqrt\alpha}\approx7.664,
$$

$$
\omega_p=25\sqrt\alpha\approx81.554.
$$

所以可取

$$
G_c(s)=
\frac{1+\dfrac{s}{7.664}}
{1+\dfrac{s}{81.554}}.
$$

校正后的开环为

$$
G(s)=
\frac{50\left(1+\dfrac{s}{7.664}\right)}
{s(0.25s+1)(0.01s+1)
\left(1+\dfrac{s}{81.554}\right)}.
$$

校核：

$$
|G(j25)|=1,
$$

$$
\gamma\approx50.97^\circ>45^\circ.
$$

### 8.2.7 第七题：复根轨迹为圆的证明

闭环特征方程为

$$
s^2+(1+K)s+2K=0.
$$

在复根轨迹上令

$$
s=x+jy,\qquad y\ne0.
$$

分离实部和虚部：

$$
x^2-y^2+(1+K)x+2K=0,
$$

$$
(2x+1+K)y=0.
$$

因为

$$
y\ne0,
$$

所以

$$
K=-2x-1.
$$

代回实部方程：

$$
x^2-y^2+(-2x)x+2(-2x-1)=0.
$$

整理：

$$
(x+2)^2+y^2=2.
$$

故复数根轨迹位于以负二为圆心、根号二为半径的圆上。

### 8.2.8 第八题：飞机系统概略根轨迹

图中两个环节发生精确约消：

$$
\frac{10}{s+10}\,K_1(0.1s+1)=K_1.
$$

故等效开环为

$$
L(s)=
\frac{K_1K_2(s+2)^2}
{(s+10)(s+100)(s^2+15s+6.25)}.
$$

令

$$
K=K_1K_2.
$$

四个极点约为

$$
-100,\quad -14.571,\quad -10,\quad -0.429,
$$

两个重合零点为

$$
-2,\quad -2.
$$

实轴根轨迹为

$$
(-100,-14.571)\cup(-10,-0.429).
$$

两条无穷远渐近线为

$$
\theta=90^\circ,\ 270^\circ,
$$

交点为

$$
\sigma_a=-60.5.
$$

有效实轴分离点约为

$$
s=-59.43.
$$

根轨迹不穿越虚轴。阻尼比零点七零七对应正负四十五度射线，可在图上与根轨迹交点处读取所需总增益。单独改变 K₁ 或 K₂ 不改变轨迹形状，只改变轨迹参数：

$$
K_2=\frac{K}{K_1},
\qquad 0.02\le K_1\le0.2.
$$

### 8.2.9 第九题：离散系统的对角标准型

给定

$$
G(z)=\frac{0.368z+0.264}{z^2-1.368z+0.368}.
$$

分母分解为

$$
z^2-1.368z+0.368=(z-1)(z-0.368).
$$

部分分式展开：

$$
G(z)=\frac1{z-1}-\frac{0.632}{z-0.368}.
$$

故一种对角标准型为

$$
x(k+1)=
\begin{bmatrix}
1&0\\
0&0.368
\end{bmatrix}x(k)+
\begin{bmatrix}
1\\1
\end{bmatrix}u(k),
$$

$$
y(k)=
\begin{bmatrix}
1&-0.632
\end{bmatrix}x(k).
$$

结构图由两个并联的一阶离散环节组成，输出分别乘以一和负零点六三二后相加。

### 8.2.10 第十题：可控、可观与非最小实现

对表达式一：

$$
A_1=
\begin{bmatrix}
0&1\\-0.4&-1.3
\end{bmatrix},\quad
B_1=
\begin{bmatrix}
1\\0
\end{bmatrix},\quad
C_1=
\begin{bmatrix}
0.8&1
\end{bmatrix}.
$$

能控矩阵满足

$$
\operatorname{rank}
\begin{bmatrix}B_1&A_1B_1\end{bmatrix}
=\operatorname{rank}
\begin{bmatrix}
1&0\\0&-0.4
\end{bmatrix}=2.
$$

能观矩阵满足

$$
\operatorname{rank}
\begin{bmatrix}
C_1\\C_1A_1
\end{bmatrix}
=\operatorname{rank}
\begin{bmatrix}
0.8&1\\-0.4&-0.5
\end{bmatrix}=1.
$$

所以表达式一完全能控，但不完全能观。

表达式二是其对偶实现：

$$
A_2=A_1^T,\qquad B_2=C_1^T,\qquad C_2=B_1^T.
$$

由对偶性可知，表达式二不完全能控，但完全能观。

两者的传递函数相同：

$$
G(s)=C_1(sI-A_1)^{-1}B_1
=\frac{0.8(s+0.8)}{(s+0.5)(s+0.8)}
=\frac{0.8}{s+0.5}.
$$

关键结论：能控性和能观性在相似变换下不变。因此这里的差异不能归因于“同一状态变量换坐标”。真正原因是两者都是二阶非最小实现：

- 表达式一把被约消模态放在不可观子空间；
- 表达式二把被约消模态放在不可控子空间；
- 它们传递函数相同，但不是同一完整状态模型的相似实现。

最小实现只有一阶：

$$
\dot x=-0.5x+u,
$$

$$
y=0.8x.
$$

---

## 8.3 2025 年

2025 年十道题的题意、判据、逐步推导、数值代入、结论和易错点见：

[9. 厦门大学 2025 年 844 真题完整解答](9.厦门大学2025年844真题逐题技巧与知识连接.md)。

---

## 8.4 2023—2024 考点归纳与 2025 衔接

| 模块 | 2023 | 2024 | 复习优先级 |
|---|---:|---:|---:|
| 电路建模、传递函数 | 有 | 有 | 高 |
| 二阶时域指标 | 有 | 有 | 高 |
| 根轨迹 | 有 | 有 | 高 |
| Bode 图与校正 | 有 | 有 | 很高 |
| Nyquist 与时滞 | 有 | 无 | 中 |
| 状态空间实现 | 有 | 有 | 很高 |
| 能控、能观 | 有 | 有 | 很高 |
| 非线性控制 | 有 | 无 | 中 |
| 离散系统 | 有 | 有 | 高 |

两年共同主线为

$$
\text{建模}
\Rightarrow
\text{稳定性与动态性能}
\Rightarrow
\text{频域或根轨迹设计}
\Rightarrow
\text{状态空间性质}.
$$

建议优先熟练以下四类闭环推导：

$$
\Phi(s)=\frac{G(s)}{1+G(s)H(s)},
$$

$$
D(s)=1+G(s)H(s)=0,
$$

$$
\mathcal C=\begin{bmatrix}B&AB&\cdots&A^{n-1}B\end{bmatrix},
$$

$$
\mathcal O=
\begin{bmatrix}
C\\CA\\\vdots\\CA^{n-1}
\end{bmatrix}.
$$
