# 2. 数学预备与教材常略去的“为什么”：隐含推导与得分点

这份笔记假定读者只学过普通高等数学：会求导、积分、解简单常微分方程，知道复数和矩阵的基本记号，但不预设学过复变函数、线性代数的抽象证明或信号与系统。每节按“结论从哪里来—中间怎样算—考试写到哪一步—容易错在哪里”整理。遇到记不牢的结论，应从定义重新推出。

标记约定：@重点 是应能闭卷推导的骨架，@易错 是公式的适用边界。

## 2.0 开始控制理论前，先补齐五件数学工具

### 2.0.1 复数、模和相角

$$
\text{控制理论把实变量频率} \omega \text{代入复变量}
$$

$$
s=\sigma+j\omega
$$

，其中

$$
j^2=-1
$$

。复数

$$
z=a+jb
$$

可以写成极坐标形式

$$
z=|z|e^{j\theta},\qquad |z|=\sqrt{a^2+b^2},\qquad
\theta=\operatorname{atan2}(b,a).
$$

欧拉公式

$$
e^{j\theta}=\cos\theta+j\sin\theta
$$

说明“指数形式”同时携带长度和角度。若

$$
z=z_1z_2
$$

，则

$$
|z|=|z_1||z_2|,\qquad \angle z=\angle z_1+\angle z_2.
$$

因此传递函数各因子的幅值相乘、相角相加；这正是 Bode 图可以逐项叠加的数学原因。

> @易错：只用
>
> $$
> \arctan(b/a)
> $$
>
> 会丢失象限；相角计算应看实部、虚部的符号，或使用
>
> $$
> \operatorname{atan2}
> $$
>
> 。

### 2.0.2 拉普拉斯变换为什么能把微分变成代数

定义

$$
X(s)=\mathcal L\{x(t)\}=\int_0^\infty x(t)e^{-st}\,dt.
$$

对导数做一次分部积分：令

$$
u=e^{-st}
$$

、

$$
dv=\dot x(t)dt
$$

，则

$$
\begin{aligned}
\mathcal L\{\dot x\}
&=\int_0^\infty \dot x(t)e^{-st}\,dt\\
&=\left[x(t)e^{-st}\right]_0^\infty
+s\int_0^\infty x(t)e^{-st}\,dt.
\end{aligned}
$$

$$
\text{在} x(t)e^{-st}\to0 \text{的收敛条件下，} \text{上式成为}
$$

$$
\boxed{\mathcal L\{\dot x\}=sX(s)-x(0).}
$$

再用一次同样的公式：

$$
\mathcal L\{\ddot x\}
=s\bigl[sX(s)-x(0)\bigr]-\dot x(0)
=s^2X(s)-sx(0)-\dot x(0).
$$

$$
\text{所以零初值时，} \text{求一次导数对应乘一个} s \text{。}
$$

$$
\text{微分方程因而变成关于} X(s) \text{、} U(s) \text{的代数方程。}
$$

传递函数必须强调零初值，正是因为非零初值会留下额外项。

### 2.0.3 矩阵只需要先掌握这些操作

$$
\text{若} A\in\mathbb R^{n\times n} \text{、} x\in\mathbb R^{n\times1} \text{，} \text{则} Ax \text{是} n\times1 \text{列向量。}
$$

矩阵乘法的内侧维数必须相等：

$$
(m\times n)(n\times p)=(m\times p).
$$

方程

$$
Mx=b
$$

在

$$
M
$$

可逆时写成

$$
x=M^{-1}b
$$

。二维矩阵

$$
M=\begin{bmatrix}a&b\\c&d\end{bmatrix}
$$

满足

$$
\det M=ad-bc,\qquad
M^{-1}=\frac1{ad-bc}\begin{bmatrix}d&-b\\-c&a\end{bmatrix}.
$$

当

$$
\det M=0
$$

时，方程可能没有唯一解。令

$$
M=\lambda I-A
$$

，条件

$$
\det(\lambda I-A)=0
$$

$$
\text{给出} A \text{的特征值；} \text{它们是系统能够自行产生的指数模态。}
$$

矩阵的秩可以先理解为“互相独立的行或列有多少个”。

### 2.0.4 对数和分贝

幅值的分贝定义为

$$
L(\omega)=20\log_{10}|G(j\omega)|.
$$

$$
\text{之所以用 20，} \text{是因为功率与电压或幅值的平方成正比，} \text{而功率比用} 10\log_{10} \text{表示：}
$$

$$
10\log_{10}\frac{P_2}{P_1}
=10\log_{10}\left(\frac{A_2}{A_1}\right)^2
=20\log_{10}\frac{A_2}{A_1}.
$$

对数把乘法变成加法：

$$
20\log_{10}|G_1G_2|
=20\log_{10}|G_1|+20\log_{10}|G_2|.
$$

所以串联环节的 Bode 幅频曲线可以逐项相加。

### 2.0.5 本套笔记的推导阅读法

看到一个新公式时，依次问四个问题：

1. 每个符号是什么量，维数是多少；
2. 公式从哪个定义或方程开始；
3. 中间用了代入、移项、求导、积分还是取极限；
4. 结论依赖零初值、闭环稳定、可逆或线性定常中的哪一项。

后文遇到超出普通高等数学的定理，会明确标出“把它当作判据使用”，并给出直观解释，不假装一步就能严格证明。

## 2.1 为什么状态矩阵上面是 0、1，最后一行是原方程系数

从一般微分方程开始：

$$
y^{(n)}+a_{n-1}y^{(n-1)}+\cdots+a_1\dot y+a_0y=bu.
$$

选相变量状态

$$
x_1=y,\quad x_2=\dot y,\quad \ldots,\quad x_n=y^{(n-1)}.
$$

逐个求导：

$$
\dot x_1=x_2,\quad \dot x_2=x_3,\quad \ldots,\quad \dot x_{n-1}=x_n.
$$

例如

$$
\dot x_1=x_2
$$

的系数形式是

$$
\dot x_1=0x_1+1x_2+0x_3+\cdots+0x_n.
$$

所以第一行必然是

$$
[0,1,0,\ldots,0]
$$

。其余各行同理，那个 1 只是把“下一阶导数”传给前一个状态，并不是背出来的常数。

最后一个状态满足

$$
\dot x_n=y^{(n)}
$$

。由原微分方程移项：

$$
y^{(n)}=-a_0y-a_1\dot y-\cdots-a_{n-1}y^{(n-1)}+bu,
$$

因此

$$
\dot x_n=-a_0x_1-a_1x_2-\cdots-a_{n-1}x_n+bu.
$$

合并各行：

$$
\boxed{
A=
\begin{bmatrix}
0&1&0&\cdots&0\\
0&0&1&\cdots&0\\
\vdots&\vdots&\vdots&\ddots&\vdots\\
0&0&0&\cdots&1\\
-a_0&-a_1&-a_2&\cdots&-a_{n-1}
\end{bmatrix},
\qquad
B=\begin{bmatrix}0\\0\\\vdots\\0\\b\end{bmatrix}.
}
$$

> @重点：上方的 1 来自状态定义之间的逐级求导；末行负号来自原方程移项；
>
> $$
> B
> $$
>
> 的末项是输入在最高阶标准化方程中的真实系数。

> @易错：原方程最高阶项前若有
>
> $$
> m\ne1
> $$
>
> ，必须先把整个方程除以
>
> $$
> m
> $$
>
> 。例如
>
> $$
> m\ddot y+c\dot y+ky=u
> $$
>
> 中，
>
> $$
> B
> $$
>
> 的末项是
>
> $$
> 1/m
> $$
>
> ，不是 1。

## 2.2 为什么输出矩阵常取第一状态，什么时候不是

输出方程是

$$
y=Cx+Du
$$

。若状态定义中

$$
x_1=y
$$

，则

$$
y=1x_1+0x_2+\cdots+0x_n,
$$

所以

$$
C=[1,0,\ldots,0],\qquad D=0.
$$

但

$$
C
$$

$$
\text{由“测量什么”决定，} \text{不由} A \text{自动决定。}
$$

例如测量速度

$$
y=\dot q=x_2
$$

时，

$$
C=[0,1,0,\ldots,0].
$$

若输出为

$$
y=2x_1-3x_2+0.5u
$$

，则

$$
C=[2,-3],\qquad D=0.5.
$$

> @重点：
>
> $$
> A
> $$
>
> 描述内部状态怎样互相影响，
>
> $$
> B
> $$
>
> 描述输入从哪里进入，
>
> $$
> C
> $$
>
> 描述哪些状态被测量，
>
> $$
> D
> $$
>
> 描述输入是否瞬时直通输出。

## 2.3 为什么传递函数的分子会进入输出矩阵

对严格真分式

$$
G(s)=\frac{b_{n-1}s^{n-1}+\cdots+b_1s+b_0}
{s^n+a_{n-1}s^{n-1}+\cdots+a_1s+a_0},
$$

$$
\text{取可控伴随型} A \text{和}
$$

$$
B=[0,\ldots,0,1]^T
$$

。由

$$
G(s)=C(sI-A)^{-1}B
$$

可得

$$
(sI-A)^{-1}B
=\frac{1}{p(s)}
\begin{bmatrix}1\\s\\\vdots\\s^{n-1}\end{bmatrix}.
$$

上面这个列向量也可以不背。令

$$
v=(sI-A)^{-1}B,\qquad (sI-A)v=B.
$$

$$
\text{写成分量后，} \text{前} n-1 \text{行依次给出}
$$

$$
sv_1-v_2=0,\quad sv_2-v_3=0,\quad\ldots,\quad
sv_{n-1}-v_n=0.
$$

因此

$$
v_2=sv_1,\quad v_3=s^2v_1,\quad\ldots,\quad v_n=s^{n-1}v_1.
$$

最后一行代入这些关系，得到

$$
\bigl(s^n+a_{n-1}s^{n-1}+\cdots+a_1s+a_0\bigr)v_1=1.
$$

所以

$$
v_1=1/p(s)
$$

$$
\text{，} \text{其余分量自然是} s^kv_1 \text{。}
$$

$$
\text{分子系数进入} C \text{，} \text{只是因为最后一步} Cv \text{正好把这些幂按系数组合起来。}
$$

若取

$$
C=[b_0,b_1,\ldots,b_{n-1}],
$$

则

$$
C
$$

与列向量相乘，恰好组成传递函数分子。

> @易错：这个
>
> $$
> C
> $$
>
> 属于特定的可控标准型，不表示所有状态实现的
>
> $$
> C
> $$
>
> 都等于分子系数。换状态坐标后
>
> $$
> A,B,C
> $$
>
> 都会变，但传递函数不变。

若分子与分母同阶，先作多项式除法：

$$
G(s)=D+\frac{\text{严格真分式部分}}{p(s)}.
$$

商

$$
D
$$

$$
\text{是直接传递项；} \text{严格真分式的修正分子再进入} C \text{。}
$$

## 2.4 为什么同一个系统可以有不同的状态空间矩阵

令新坐标

$$
z=T^{-1}x
$$

$$
\text{，} \text{其中} T \text{可逆。}
$$

因为

$$
x=Tz
$$

，代入

$$
\dot x=Ax+Bu
$$

：

$$
T\dot z=ATz+Bu.
$$

$$
\text{左乘} T^{-1} \text{：}
$$

$$
\dot z=T^{-1}ATz+T^{-1}Bu,\qquad y=CTz+Du.
$$

所以

$$
\bar A=T^{-1}AT,\quad \bar B=T^{-1}B,\quad
\bar C=CT,\quad \bar D=D.
$$

相似变换不改变特征值，也不改变传递函数：

$$
\bar C(sI-\bar A)^{-1}\bar B+D
=C(sI-A)^{-1}B+D.
$$

> @重点：状态变量是内部坐标，不唯一；极点、传递函数、能控性和能观性才是坐标变换下不变的系统性质。

## 2.5 为什么状态响应里出现矩阵指数

仿照标量指数定义

$$
e^{At}=I+At+\frac{A^2t^2}{2!}+\cdots.
$$

逐项求导：

$$
\frac{d}{dt}e^{At}
=A+A^2t+\frac{A^3t^2}{2!}+\cdots
=Ae^{At}.
$$

且

$$
e^{A0}=I
$$

，因此

$$
x(t)=e^{At}x(0)
$$

同时满足微分方程和初值。

对

$$
\dot x=Ax+Bu
$$

$$
\text{，} \text{不能直接把} Bu \text{当常数。}
$$

仿照一阶方程的积分因子法，令

$$
x=e^{At}v
$$

。由乘积求导

$$
\dot x=Ae^{At}v+e^{At}\dot v.
$$

另一方面，原方程右侧为

$$
Ax+Bu=Ae^{At}v+Bu.
$$

$$
\text{两式相等，} \text{消去共同项} Ae^{At}v \text{：}
$$

$$
e^{At}\dot v=Bu,\qquad \dot v=e^{-At}Bu.
$$

$$
\text{从} 0 \text{到} t \text{积分：}
$$

$$
v(t)-v(0)=\int_0^t e^{-A\tau}Bu(\tau)\,d\tau.
$$

因为

$$
x(0)=v(0)
$$

$$
\text{，} \text{再左乘} e^{At} \text{，} \text{并使用同一矩阵} A \text{的指数满足}
$$

$$
e^{At}e^{-A\tau}=e^{A(t-\tau)}
$$

，得到

$$
\boxed{x(t)=e^{At}x(0)+\int_0^te^{A(t-\tau)}Bu(\tau)\,d\tau.}
$$

## 2.6 为什么极点等于状态矩阵的特征值

零初值拉普拉斯变换给出

$$
X(s)=(sI-A)^{-1}BU(s).
$$

矩阵逆可写成

$$
(sI-A)^{-1}=\frac{\operatorname{adj}(sI-A)}{\det(sI-A)}.
$$

$$
\text{其分母为} \det(sI-A) \text{，} \text{令其为零正是} A \text{的特征方程。}
$$

$$
\text{因此内部自然模态由} A \text{的特征值决定。}
$$

> @易错：传递函数中可能出现极零相消，使某些特征值看不见。只有最小实现中，传递函数极点才与
>
> $$
> A
> $$
>
> 的全部特征值一一对应。

## 2.7 为什么状态反馈会改变闭环状态矩阵

取控制律

$$
u=-Kx+Nr
$$

，直接代入：

$$
\dot x=Ax+B(-Kx+Nr)
=(A-BK)x+BNr.
$$

$$
\text{因此闭环内部矩阵是} A-BK \text{。}
$$

负号来自所选负反馈控制律；若控制律定义成

$$
u=Kx
$$

$$
\text{，} \text{公式自然变成} A+BK \text{。}
$$

> @重点：不是反馈直接修改
>
> $$
> A
> $$
>
> ；而是输入先乘
>
> $$
> B
> $$
>
> ，所以反馈产生的修正量必然是
>
> $$
> BK
> $$
>
> 。

## 2.8 为什么观测器会产生特定的误差矩阵

全维观测器为

$$
\dot{\hat x}=A\hat x+Bu+L(y-C\hat x).
$$

定义

$$
e=x-\hat x
$$

。由于

$$
y=Cx
$$

，对象方程与观测器相减：

$$
\dot e=Ax+Bu-A\hat x-Bu-L(Cx-C\hat x)
=(A-LC)e.
$$

$$
\text{输入项} Bu \text{被消去，} \text{是因为对象和观测器使用同一个已知输入。}
$$

$$
\text{输出残差通过} L \text{注入，} \text{用来纠正估计。}
$$

> @易错：若
>
> $$
> D\ne0
> $$
>
> ，创新项应写成
>
> $$
> y-C\hat x-Du
> $$
>
> ，否则输入直通项会被误当成状态估计误差。

## 2.9 为什么能控矩阵由输入方向及其传播方向组成

$$
\text{输入经过} B \text{进入系统，} \text{随后被内部动力学} A \text{传播，} \text{因此可能到达的方向依次是}
$$

$$
B,\ AB,\ A^2B,\ \ldots.
$$

由 Cayley--Hamilton 定理，更高次幂不会增加新方向。所有可达方向张成

$$
\operatorname{span}\{B,AB,\ldots,A^{n-1}B\}.
$$

$$
\text{若其秩为} n \text{，} \text{这些方向覆盖整个状态空间：}
$$

$$
\boxed{\mathcal C=[B\ AB\ \cdots\ A^{n-1}B],\qquad
\operatorname{rank}\mathcal C=n.}
$$

更具体地，从零初态出发，状态响应是

$$
x(t)=\int_0^t e^{A(t-\tau)}Bu(\tau)\,d\tau.
$$

把矩阵指数展开：

$$
e^{A(t-\tau)}B
=B+(t-\tau)AB+\frac{(t-\tau)^2}{2!}A^2B+\cdots.
$$

$$
\text{积分只会改变每个方向前面的标量系数，} \text{因此无论怎样选择输入，} x(t) \text{都只能落在}
$$

$$
\operatorname{span}\{B,AB,A^2B,\ldots\}
$$

之中。Cayley--Hamilton 定理在这里可先当作一个线性代数结论：

$$
n\times n
$$

$$
\text{矩阵的} A^n \text{能用}
$$

$$
I,A,\ldots,A^{n-1}
$$

的线性组合表示。

$$
\text{所以检查到} A^{n-1}B \text{已经足够。}
$$

> @重点：
>
> $$
> B
> $$
>
> 是输入直接推动的方向，
>
> $$
> AB
> $$
>
> 是该影响经过一次内部传播后的方向；能控矩阵不是凭空拼接出来的。

## 2.10 为什么能观矩阵由连续的输出信息组成

先看零输入系统

$$
y=Cx
$$

。因为

$$
\dot x=Ax
$$

，对输出求导：

$$
\dot y=CAx,\qquad \ddot y=CA^2x.
$$

把初始时刻的输出及其导数叠起来：

$$
\begin{bmatrix}
y(0)\\\dot y(0)\\\vdots\\y^{(n-1)}(0)
\end{bmatrix}
=
\begin{bmatrix}
C\\CA\\\vdots\\CA^{n-1}
\end{bmatrix}x(0).
$$

若有已知输入，第一阶导数会变成

$$
\dot y=CAx+CBu,
$$

$$
\text{第二阶还会出现} CABu \text{和输入导数项。}
$$

$$
\begin{gathered}
\text{由于} u \text{是已知量，} \text{这些项原则上可以从测量中扣除；} \\
\text{判断“未知初始状态能否被看见”的核心矩阵仍由}
\end{gathered}
$$

$$
C,CA,\ldots
$$

组成。考试中常先令

$$
u=0
$$

，正是为了把这个结构显示得最清楚。

若右侧矩阵满列秩，初始状态可由输出信息唯一恢复；若不满秩，就存在某个非零状态方向始终不可见。

> @重点：能控性沿
>
> $$
> B\to AB\to\cdots
> $$
>
> 向外传播；能观性沿
>
> $$
> C\to CA\to\cdots
> $$
>
> 把内部状态投影到输出。

## 2.11 为什么零阶保持会得到精确离散模型

连续状态响应在一个采样区间上为

$$
x((k+1)T)=e^{AT}x(kT)
+\int_{kT}^{(k+1)T}e^{A((k+1)T-\tau)}Bu(\tau)\,d\tau.
$$

$$
\text{零阶保持器让区间内输入恒为} u_k \text{。}
$$

更换积分变量后

$$
x_{k+1}=e^{AT}x_k+
\left(\int_0^Te^{A\tau}B\,d\tau\right)u_k.
$$

所以

$$
\boxed{A_d=e^{AT},\qquad B_d=\int_0^Te^{A\tau}B\,d\tau.}
$$

> @易错：一般不能写成
>
> $$
> A_d=I+AT,\ B_d=BT
> $$
>
> 。那只是小采样周期下的一阶近似。

## 2.12 为什么负反馈闭环分母出现加号

负反馈结构满足

$$
e=r-Hy,\qquad y=Ge.
$$

代入并移项：

$$
y=G(r-Hy)=Gr-GHy,
$$

$$
(1+GH)y=Gr.
$$

因此

$$
\boxed{\frac{Y}{R}=\frac{G}{1+GH}.}
$$

分母中的加号来自负反馈关系里的减号在移项后变成加号。

$$
\text{若结构本身是正反馈，} \text{分母才是} 1-GH \text{。}
$$

## 2.13 为什么系统型别决定阶跃、斜坡、抛物线误差

单位反馈下

$$
E(s)=\frac{R(s)}{1+G(s)}.
$$

$$
\text{若开环在原点有} \nu \text{个积分环节，} \text{低频} G(s)\sim K/s^\nu \text{。}
$$

$$
\text{对} m \text{阶多项式输入，} R(s) \text{与} 1/s^{m+1} \text{成正比。}
$$

由终值定理

$$
e_{ss}\sim\lim_{s\to0}
\frac{s}{1+K/s^\nu}\frac{1}{s^{m+1}}
\sim\frac{1}{K}s^{\nu-m}.
$$

这里用到的终值定理是

$$
\lim_{t\to\infty}e(t)=\lim_{s\to0}sE(s).
$$

$$
\text{它不是对任意} E(s) \text{都成立。}
$$

$$
\begin{gathered}
\text{直观上，} \text{若误差中还含有不衰减的振荡或发散指数，} \text{时间终值本来就不存在；} \\
\text{常用的充分检查是} sE(s) \text{的全部极点都在左半平面。}
\end{gathered}
$$

于是稳态误差题的第一步必须先确认闭环稳定。

于是：

$$
\nu>m
$$

- 误差趋于 0；

$$
\nu=m
$$

- 误差为有限常数；

$$
\nu<m
$$

- 误差发散。

这就是 0、1、2 型系统分别对应位置、速度、加速度误差系数的来源。

> @易错：这套结论依赖闭环稳定和低频极限存在；不能只数积分环节而跳过稳定性检查。

## 2.14 为什么二阶系统的超调量是指数公式

标准欠阻尼二阶系统极点为

$$
s_{1,2}=-\zeta\omega_n\pm j\omega_n\sqrt{1-\zeta^2}.
$$

记

$$
\omega_d=\omega_n\sqrt{1-\zeta^2}
$$

。单位直流增益时的单位阶跃响应可由部分分式得到

$$
y(t)=1-e^{-\zeta\omega_nt}
\left[\cos\omega_dt+
\frac{\zeta}{\sqrt{1-\zeta^2}}\sin\omega_dt\right].
$$

对它求导并合并同类项：

$$
\dot y(t)=
\frac{\omega_n}{\sqrt{1-\zeta^2}}
e^{-\zeta\omega_nt}\sin\omega_dt.
$$

指数项和前面的常数不为零，所以极值条件

$$
\dot y(t)=0
$$

等价于

$$
\sin\omega_dt=0
$$

。第一个非零极值时刻是

$$
t_p=\frac{\pi}{\omega_d}.
$$

把

$$
t_p
$$

代回响应。此时

$$
\sin\pi=0
$$

、

$$
\cos\pi=-1
$$

，所以

$$
\boxed{M_p=e^{-\zeta\omega_nt_p}
=e^{-\frac{\pi\zeta}{\sqrt{1-\zeta^2}}}.}
$$

$$
\omega_n
$$

$$
\text{被约掉，} \text{所以百分比超调主要由阻尼比决定；} \omega_n \text{主要控制时间尺度。}
$$

> @易错：该公式针对
>
> $$
> 0<\zeta<1
> $$
>
> 的标准欠阻尼二阶系统，并默认比较的是单位稳态值。若有零点、额外极点或直流增益不为 1，不能直接套用。

## 2.15 为什么二阶系统调节时间可以用指数包络近似

$$
\text{误差包络近似按} e^{-\zeta\omega_nt} \text{衰减。}
$$

采用 2% 误差带：

$$
e^{-\zeta\omega_nt_s}\approx0.02,
$$

$$
t_s\approx\frac{-\ln0.02}{\zeta\omega_n}
\approx\frac{3.91}{\zeta\omega_n}
\approx\frac{4}{\zeta\omega_n}.
$$

$$
\text{5\% 误差带给出} t_s\approx3/(\zeta\omega_n) \text{。}
$$

> @重点：常数 4 不是固定定理，而是 2% 误差带与指数包络近似共同产生的。

## 2.16 为什么根轨迹有角度条件和幅值条件

闭环特征方程

$$
1+KG_0(s)=0
$$

，所以根轨迹上的点满足

$$
KG_0(s)=-1=1\angle(2k+1)\pi.
$$

比较幅值与相角：

$$
\boxed{\angle G_0(s)=(2k+1)\pi,\qquad
K=\frac{1}{|G_0(s)|}.}
$$

若

$$
G_0(s)=\frac{\prod_i(s-z_i)}{\prod_k(s-p_k)},
$$

则复数乘除的相角分别相加、相减：

$$
\angle G_0(s)=\sum_i\angle(s-z_i)-\sum_k\angle(s-p_k).
$$

$$
\text{几何上，} s-z_i \text{是从零点} z_i \text{指向候选点} s \text{的向量，} s-p_k \text{同理。}
$$

$$
\text{因此角度条件可以直接在} s \text{平面上量角，} \text{而幅值条件把各向量长度代入后求出} K \text{。}
$$

角度条件决定某点是否在根轨迹上，幅值条件决定对应增益。

## 2.17 为什么 Bode 图的一阶极点带来负斜率

一阶极点因子为

$$
\frac{1}{1+j\omega/\omega_p}.
$$

当

$$
\omega\gg\omega_p
$$

$$
\text{时，} \text{其幅值近似为} \omega_p/\omega \text{。}
$$

取分贝：

$$
20\log_{10}\frac{\omega_p}{\omega}
=-20\log_{10}\frac{\omega}{\omega_p}.
$$

$$
\text{频率增大 10 倍，} \text{对数增加 1，} \text{幅值下降 20 dB，} \text{因此斜率是} -20 \text{dB/dec。}
$$

$$
\text{一个一阶零点贡献} +20 \text{dB/dec；} \text{二阶因子贡献} \pm40 \text{dB/dec。}
$$

## 2.18 为什么 Nyquist 判据要围绕临界点计数

闭环特征方程是

$$
1+L(s)=0
$$

。

$$
\begin{gathered}
\text{辩值原理说明：} \text{沿 Nyquist 围线走一周，} 1+L(s) \\
\text{绕原点的净圈数等于其右半平面零点数减去极点数。}
\end{gathered}
$$

辩值原理属于复变函数内容，仅学过普通高等数学时可以把它当作判据使用。直观理解是：一个连续复函数沿闭合边界走一圈时，曲线绕原点的净转角会记录边界内部“零点造成的正旋转”和“极点造成的反旋转”之差。Nyquist 方法把“直接求高阶闭环根”换成了“观察开环频率曲线绕点的次数”。

$$
\text{把曲线} 1+L(s) \text{平移后看成} L(s) \text{，} \text{“} 1+L \text{绕原点”等价于“} L \text{绕} -1 \text{点”。}
$$

若采用顺时针包围为正的约定，

$$
\boxed{N=Z-P.}
$$

$$
P
$$

$$
\text{是开环右半平面极点数，} Z \text{是闭环右半平面极点数；} \text{闭环稳定要求}
$$

$$
Z=0
$$

。

> @易错：不同教材对方向和
>
> $$
> N
> $$
>
> 的正负定义可能相反。不要孤立背公式，必须先声明符号约定。

## 2.19 一页式“为什么”答题模板

遇到教材直接给公式的题，可按以下顺序补齐得分步骤：

1. 写定义：状态怎么选、误差怎么定义、输入输出关系是什么；
2. 逐行展开：先写标量方程，再合并成矩阵；
3. 标出来源：1 来自逐级求导，负号来自移项，输入矩阵来自输入位置；
4. 写成立条件：零初值、闭环稳定、严格真分式、无直接传递项或指定的采样保持方式；
5. 最后验算：矩阵维数、特征多项式、直流增益和稳定区域。

> @重点：真正能稳定得分的不是多背一个公式，而是能从“定义 → 代入 → 移项 → 合并”四步把公式重新造出来。
