# 4. 经典控制知识框架速查

## 4.1 主线

> @重点：任何计算先写闭环特征方程；稳定性是终值定理、频率裕度和稳态误差公式的共同前提。

$$
\mathrm{Differential\ Equation}
\Rightarrow G(s)
\Rightarrow \mathrm{Characteristic\ Equation}
\Rightarrow \mathrm{Analysis}
\Rightarrow \mathrm{Compensation}.
$$

## 4.2 反馈闭环

$$
\frac{Y(s)}{R(s)}=\frac{G(s)}{1+G(s)H(s)}.
$$

$$
1+G(s)H(s)=0.
$$

## 4.3 一阶与二阶系统

$$
G_1(s)=\frac{K}{Ts+1}.
$$

$$
y_{step}(t)=K\left(1-e^{-t/T}\right).
$$

$$
G_2(s)=\frac{\omega_n^2}{s^2+2\zeta\omega_ns+\omega_n^2}.
$$

$$
M_p=e^{-\frac{\pi\zeta}{\sqrt{1-\zeta^2}}}.
$$

$$
t_s\approx\frac{4}{\zeta\omega_n}.
$$

## 4.4 稳定性

闭环全部极点位于开左半平面时，连续系统渐近稳定。

劳斯表第一列符号变化次数等于右半平面根数。

指定

$$
\operatorname{Re}(s)<-\sigma
$$

时，先令

$$
s=z-\sigma
$$

，再对

$$
z
$$

多项式作劳斯表。全零行用上一行构造辅助多项式并求导。

## 4.5 稳态误差

$$
e_{ss}=\lim_{s\to0}sE(s).
$$

$$
K_p=\lim_{s\to0}G(s),
\qquad
K_v=\lim_{s\to0}sG(s),
\qquad
K_a=\lim_{s\to0}s^2G(s).
$$

## 4.6 根轨迹

$$
\angle G_0(s)=(2k+1)\pi.
$$

$$
K=\frac{1}{\left|G_0(s)\right|}.
$$

## 4.7 Bode 图与稳定裕度

$$
20\log_{10}\left|G(j\omega)\right|.
$$

$$
\mathrm{PM}=180^\circ+\angle L(j\omega_{gc}).
$$

$$
\mathrm{GM}=\frac{1}{\left|L(j\omega_{pc})\right|}.
$$

## 4.8 Nyquist 判据

按顺时针包围负一为正的约定：

$$
N=Z-P.
$$

闭环稳定要求

$$
Z=0.
$$

## 4.9 PID

$$
C(s)=K_p+\frac{K_i}{s}+K_ds.
$$

比例提速，积分消除恒值误差，微分增加阻尼但放大噪声。

## 4.10 超前与滞后校正

$$
C_{lead}(s)=K\frac{1+Ts}{1+\alpha Ts},
\qquad 0<\alpha<1.
$$

$$
C_{lag}(s)=K\frac{1+Ts}{1+\beta Ts},
\qquad \beta>1.
$$

超前校正参数：

$$
\alpha=\frac{1-\sin\phi_{max}}{1+\sin\phi_{max}}.
$$

$$
\omega_m=\frac{1}{T\sqrt{\alpha}}.
$$

$$
\left|\frac{C_{lead}(j\omega_m)}{K}\right|
=\frac{1}{\sqrt{\alpha}}.
$$

超前网络的极点与零点：

$$
z=-\frac{1}{T},
\qquad
p=-\frac{1}{\alpha T}.
$$

滞后网络的极点与零点：

$$
z=-\frac{1}{T},
\qquad
p=-\frac{1}{\beta T}.
$$

若取

$$
K=\beta,
$$

则滞后网络低频增益提高 β 倍，高频增益趋于一。

超前改善相位裕度与速度；滞后提高低频增益与稳态精度。

> @易错：先由稳态指标确定总增益，再由动态指标设计超前/滞后；最后必须回代实际穿越频率与相位裕度。

## 4.11 结构图与梅逊公式

$$
T=\frac{\sum P_k\Delta_k}{\Delta},
\qquad
\Delta=1-\sum L_i+\sum L_iL_j-\cdots.
$$

回路乘积只取互不接触回路；多个独立输入分别置零求通道后叠加。

其中：

$$
\Delta_k
=
\text{删去与第 }k\text{ 条前向通路接触的回路后形成的图行列式}.
$$

计算顺序：前向通路、全部回路、不接触回路组、图行列式、各通路余因子、总传递增益。完整证明和例题见第 10 份专题。

## 4.12 离散系统

$$
z_i=e^{s_iT},
\qquad |z_i|<1.
$$

二阶

$$
p(z)=z^2+a_1z+a_0
$$

的 Jury 条件：

$$
1+a_1+a_0>0,
\quad 1-a_1+a_0>0,
\quad 1-a_0>0.
$$

> @重点：连续看左半平面，离散看单位圆；不要把
>
> $$
> z
> $$
>
> 当作
>
> $$
> s
> $$
>
> 直接套劳斯。

## 4.13 非线性系统

平衡点满足

$$
f(x_e)=0
$$

，局部线性化矩阵为

$$
J=\left.\frac{\partial f}{\partial x}\right|_{x_e}.
$$

描述函数预测极限环：

$$
G(j\omega)=-\frac{1}{N(A)}.
$$

该结论是基波近似，不是严格存在性证明。

## 4.14 时滞与非最小相位

$$
e^{-Ls}:\quad |G|=1,\quad \angle G=-\omega L.
$$

右半平面零点和时滞增加相位滞后，限制闭环带宽；零点影响性能但不直接决定闭环稳定性。

## 4.15 考场总检查

1. 输入与输出通道是否写对；
2. 闭环是否稳定；
3. 终值定理前提是否满足；
4. 连续/离散稳定区域是否混淆；
5. 数值结果是否回代特征方程或原传递函数。

完整推导见：[3. 经典控制理论](3.经典控制理论（公理化推导与简例）.md)。
